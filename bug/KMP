#include<bits/stdc++.h>
using namespace std;
string s,t;
int f[500005]={};
vector<int> ans;
void build(){
    int q=-1;
    f[0]=-1;
    for(int i=1;i<t.size();i++){
        while(q!=-1&&t[q+1]!=t[i]){
            q=f[q];
        }
        if(t[q+1]==t[i]){
            q++;
        }
        f[i]=q;
    }
}
void check(){
    int q=-1;
    for(int i=0;i<s.size();i++){
        while(q!=-1&&t[q+1]!=s[i]){
            q=f[q];
        }
        if(t[q+1]==s[i]){
            q++;
        }
//        cout << q<<" "<<(q==t.size()-1)<<endl;
        if(q==t.size()-1){
            ans.push_back(i-q);
//            cout << i<<" "<<q<<endl;
//            i-=f[q]+1;
            q=f[q];
//            cout << i<<" "<<q<<endl;
        }
    }
}
int main(){
    cin >> t>>s;
    build();
//    for(int i=0;i<t.size();i++){
//        cout << f[i]<<" ";
//    }
//    cout << endl;

    check();
    if(ans.size()==0){cout << endl;}
    for(int i=0;i<ans.size();i++){
        cout << ans[i]<<" ";
    }
}
